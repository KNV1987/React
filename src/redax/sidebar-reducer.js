let initialState = {
  friends: [
    { id: 1, name: "Nick" },
    { id: 2, name: "Tatsia" },
    { id: 3, name: "Dasha" },
  ],
};

const sidebarReducer = (state = initialState, action) => {
  return state;
};

export default sidebarReducer;
